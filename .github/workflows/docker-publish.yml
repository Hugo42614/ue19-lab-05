name: Build and Publish Docker Image

on:
  push:
    branches:
      - main

jobs:
  build_and_publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          username: ${{ github.actor }}  # Utilise votre nom d'utilisateur GitHub
          password: ${{ secrets.CR_PAT }}  # Utilise le token GitHub comme mot de passe

      - name: Build Docker image
        run: |
          docker build -t ghcr.io/hugo42614/ue19-lab-05:latest .

      - name: Push Docker image
        run: |
          docker push ghcr.io/hugo42614/ue19-lab-05:latest
